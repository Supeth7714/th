# Windows 사용 가이드 - MRO 업무 관리 프로그램

## 📋 목차
1. [사전 준비](#사전-준비)
2. [설치 방법](#설치-방법)
3. [프로그램 시작](#프로그램-시작)
4. [사용 방법](#사용-방법)
5. [종료 방법](#종료-방법)
6. [문제 해결](#문제-해결)

---

## 🔧 사전 준비

### Node.js 설치 (최초 1회만)

1. **Node.js 다운로드**
   - 웹사이트: https://nodejs.org/
   - **LTS 버전** (왼쪽 초록색 버튼) 클릭하여 다운로드

2. **설치 실행**
   - 다운로드한 파일 실행
   - "Next" 버튼을 계속 클릭하여 설치
   - 모든 기본 설정 그대로 사용 (체크박스 변경 불필요)

3. **설치 확인**
   - 설치 완료 후 컴퓨터 **재시작**
   - 명령 프롬프트(cmd)를 열어 다음 명령어 실행:
     ```
     node --version
     ```
   - 버전 번호가 나오면 설치 성공!

---

## 📥 설치 방법

### 1단계: 프로그램 폴더 준비
- 이 프로그램의 모든 파일이 있는 폴더를 원하는 위치에 저장하세요.
- 예: `C:\MRO_Program\`

### 2단계: 설치 스크립트 실행
1. 프로그램 폴더에서 **`install.bat`** 파일을 찾습니다.
2. **더블클릭**하여 실행합니다.
3. 자동으로 다음 작업이 진행됩니다:
   - Node.js 설치 확인
   - 필요한 패키지 다운로드 및 설치
   - 데이터베이스 초기화 (샘플 데이터 포함)

4. "설치가 완료되었습니다!" 메시지가 나오면 성공!

> **⏱️ 소요 시간**: 인터넷 속도에 따라 1~5분 정도 걸립니다.

---

## 🚀 프로그램 시작

### 간편 시작 방법
1. 프로그램 폴더에서 **`start.bat`** 파일을 찾습니다.
2. **더블클릭**하여 실행합니다.
3. 검은색 명령 프롬프트 창이 열리고 서버가 시작됩니다.
4. 약 5초 후 웹 브라우저가 자동으로 열립니다.
5. 프로그램 화면이 나타납니다!

### 수동 접속 방법
브라우저가 자동으로 열리지 않으면:
- 웹 브라우저(Chrome, Edge 등)를 열고
- 주소창에 다음 주소 입력: **http://localhost:3000**

---

## 💡 사용 방법

### 대시보드
- 프로그램을 열면 가장 먼저 보이는 화면
- 전체 현황을 한눈에 확인:
  - 총 부자재 수
  - 재고 부족 품목
  - 진행 중인 발주 건수
  - 등록된 공급업체 수
  - 최근 입고 내역

### 주요 기능 메뉴

#### 1️⃣ 부자재 관리
- **용도**: 관리할 자재의 기본 정보를 등록
- **버튼**:
  - `새 부자재 추가`: 새로운 자재 등록
  - `수정`: 기존 자재 정보 변경
  - `삭제`: 자재 삭제
- **항목**:
  - 자재 코드 (예: MAT001)
  - 자재명
  - 규격
  - 단위 (예: EA, KG, M)
  - 표준 단가
  - 안전 재고량

#### 2️⃣ 공급업체 관리
- **용도**: 거래하는 협력업체 정보 관리
- **항목**:
  - 업체 코드
  - 업체명
  - 담당자
  - 연락처
  - 이메일
  - 주소

#### 3️⃣ 발주 관리
- **용도**: 자재 구매 주문 생성 및 관리
- **절차**:
  1. `새 발주 생성` 클릭
  2. 공급업체 선택
  3. 발주 날짜 및 입고 예정일 입력
  4. 자재 선택 및 수량 입력 (여러 자재 추가 가능)
  5. `발주 생성` 클릭
- **상태**:
  - 🔵 대기 중: 아직 입고 안 됨
  - 🟡 부분 입고: 일부만 입고됨
  - 🟢 완료: 모두 입고 완료

#### 4️⃣ 입고 관리
- **용도**: 주문한 자재가 도착했을 때 입고 처리
- **절차**:
  1. `새 입고 처리` 클릭
  2. 발주 번호 입력 (예: PO-20260109-0001)
  3. 입고 날짜 입력
  4. 각 자재별 입고 수량 입력
  5. 검수 결과 선택 (합격/불합격)
  6. `입고 처리` 클릭
- **자동 처리**:
  - 재고가 자동으로 증가
  - 발주 상태가 자동으로 업데이트

#### 5️⃣ 재고 현황
- **용도**: 현재 보유 중인 자재 재고 확인
- **상태 표시**:
  - 🟢 정상: 재고가 안전 재고량 이상
  - 🟡 부족: 재고가 안전 재고량 미만
  - 🔴 없음: 재고가 0
- **거래 내역**: 각 자재를 클릭하면 입출고 이력 확인 가능

---

## 🛑 종료 방법

### 1단계: 브라우저 닫기
- 웹 브라우저의 X 버튼을 클릭하여 닫습니다.

### 2단계: 서버 종료
1. 검은색 명령 프롬프트 창으로 이동
2. 키보드에서 **Ctrl + C** 키를 동시에 누릅니다
3. "일괄 작업을 끝내시겠습니까 (Y/N)?" 메시지가 나오면
4. **Y**를 입력하고 **Enter**를 누릅니다
5. 창이 닫히면 완전히 종료된 것입니다

> **⚠️ 주의**: 명령 프롬프트 창을 그냥 X로 닫아도 되지만, Ctrl+C로 정상 종료하는 것이 좋습니다.

---

## 🔍 문제 해결

### 문제 1: "Node.js가 설치되어 있지 않습니다"
**원인**: Node.js가 설치되지 않았거나 환경 변수가 설정되지 않음
**해결**:
1. Node.js를 다시 설치합니다 (위의 "사전 준비" 참고)
2. 컴퓨터를 재시작합니다
3. `install.bat`을 다시 실행합니다

### 문제 2: "필요한 패키지가 설치되어 있지 않습니다"
**원인**: `install.bat`을 실행하지 않았거나 설치 중 오류 발생
**해결**:
1. `install.bat`을 다시 실행합니다
2. 오류 메시지가 나오면 인터넷 연결 확인
3. 관리자 권한으로 실행해봅니다 (파일 우클릭 → "관리자 권한으로 실행")

### 문제 3: 브라우저가 자동으로 열리지 않음
**원인**: 기본 브라우저 설정 문제
**해결**:
- 수동으로 브라우저를 열고 http://localhost:3000 접속

### 문제 4: "포트 3000이 이미 사용 중입니다" (EADDRINUSE)
**원인**: 다른 프로그램이 3000번 포트를 사용 중
**해결**:
1. 다른 프로그램을 종료합니다
2. 또는 명령 프롬프트에서 다음 명령어 실행:
   ```
   netstat -ano | findstr :3000
   taskkill /PID [PID번호] /F
   ```

### 문제 5: 프로그램 화면이 깨져 보임
**원인**: 브라우저 캐시 문제
**해결**:
- 브라우저에서 **Ctrl + F5** (강력 새로고침)
- 또는 브라우저 캐시 삭제 후 재접속

### 문제 6: 데이터가 사라짐
**원인**: `inventory.db` 파일 삭제 또는 손상
**해결**:
- `init-db`를 다시 실행하면 샘플 데이터로 복구됩니다
- 백업 파일이 있다면 복원합니다

---

## 📂 데이터 백업 방법

### 중요한 파일: `inventory.db`
- 위치: 프로그램 폴더의 루트 디렉토리
- 내용: 모든 자재, 발주, 입고, 재고 데이터

### 백업 방법
1. 프로그램을 종료합니다
2. `inventory.db` 파일을 복사합니다
3. 안전한 곳에 백업 파일을 저장합니다
   - 예: `inventory_backup_2026-01-09.db`

### 복원 방법
1. 프로그램을 종료합니다
2. 백업한 `inventory.db` 파일을 원래 위치에 복사합니다
3. 프로그램을 다시 시작합니다

---

## 📞 추가 도움말

### 시스템 요구사항
- **운영체제**: Windows 10 이상 (Windows 11 권장)
- **RAM**: 최소 2GB (4GB 이상 권장)
- **디스크 공간**: 최소 500MB
- **인터넷**: 최초 설치 시에만 필요

### 권장 브라우저
- Google Chrome (권장)
- Microsoft Edge
- Mozilla Firefox

### 네트워크 설정
- 기본 포트: 3000
- 같은 네트워크의 다른 PC에서 접속하려면:
  - 서버 PC의 IP 주소 확인 (예: 192.168.0.100)
  - 다른 PC에서 `http://192.168.0.100:3000` 접속
  - Windows 방화벽 예외 설정 필요

---

## ✅ 빠른 체크리스트

설치 전:
- [ ] Node.js 설치 완료
- [ ] 프로그램 파일 다운로드 완료
- [ ] 인터넷 연결 확인

첫 실행 시:
- [ ] `install.bat` 실행 완료
- [ ] "설치가 완료되었습니다" 메시지 확인
- [ ] `start.bat` 실행

매일 사용 시:
- [ ] `start.bat` 더블클릭
- [ ] 브라우저에서 http://localhost:3000 접속
- [ ] 업무 처리
- [ ] 종료 시 Ctrl+C로 서버 종료

---

**🎉 이제 MRO 업무 관리 프로그램을 사용할 준비가 완료되었습니다!**
